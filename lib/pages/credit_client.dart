import 'package:flutter/material.dart';

class CreditClient extends StatelessWidget {
  const  CreditClient({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SingleChildScrollView(
        child: Text("HHHHHHHHH"),
      ),
    );
  }
}