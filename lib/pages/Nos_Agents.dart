import 'package:flutter/material.dart';

class NosAgents extends StatelessWidget {
  const NosAgents({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: SingleChildScrollView(
      child: Padding(padding: const EdgeInsets.symmetric(vertical: 50.0),
      child: Column(
        children: [
          Text("HELP AGENTS",style: TextStyle(fontSize: 50),)
        ],
      ),
      
      
      
      
      
      
      ),
    ),);
  }
}