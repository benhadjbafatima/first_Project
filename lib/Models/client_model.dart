class Client {
  final String name;
  final String phoneNumber;
  final String address; // Note the change from "Adress" to "address" for consistency

  Client({
    required this.name,
    required this.phoneNumber,
    required this.address,
  });
}
